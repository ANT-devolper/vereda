package dev.vereda

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Example local unit test, running on the development machine (JVM).
 *
 * This project follows TDD: feature code starts from a failing test like this one.
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
